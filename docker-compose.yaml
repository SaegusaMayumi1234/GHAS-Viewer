services:
  ghas-viewer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ghas-viewer
    environment:
      - PORT=${PORT:-8080}
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    restart: unless-stopped
